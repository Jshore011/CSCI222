#include "Header.h"

using namespace std;

int main(int argc, char* argv[])
{
	
	
	
	
	
	
	
	
	
	
	/*
	const int numberSize = 3 * 3;
	int number[numberSize];

	
	
	string line;
	int index = 0;

	ifstream myfile("Input.txt");
	
	while (!myfile.eof())
	{
		getline(myfile, line);
		if (line[0] >= '0' && line[0] <= '9')
		{
			stringstream ss = stringstream(line);
			while (!ss.eof())
			{
				ss >> number[index++];
			}
		}
			
	}

	//Matrix test(number[0], number[1], number[2], number[3], number[4], number[5], number[6], number[7], number[8]);
	//cout << test;

	myfile.close();
	//}
	//else cout << "Unable to open file";
	
	//Matrix A(1, 2, 3, 2, 3, 4, 5, 6, 7);
	//Matrix B(1, 1, 1, 1, 0, 0, 1, 2, 1);
	//Matrix C, D, E, F, G, H, I, K, L;

	//L = K = A; // L.operator=(K.operator=(A));

	//D = A + B;
	//E = A - B;
	//C = A * B;
	//F = A ^ 2;
	//G = ~A; // A.operator~();
	//H = ++A; // pre-increment
	// // post-increment A.operator++(1); //

	//cout << "A = \n" << A << endl;

	//I = A--;
	//cout << "B = \n" << B << endl;
	//cout << "A*B = \n" << C << endl;
	//cout << "A+B = \n" << D << endl;
	//cout << "A-B = \n" << E << endl;
	//cout << "A^2 = \n" << F << endl;
	//cout << "A^T = \n" << G << endl;
	//cout << "A++ = \n" << H << endl;
	//cout << "I = \n" << I << "\nA-- = \n" << A << endl;
	//*/

	//

	/*cout << "Enter 9 numbers: ";

	for (int i = 0; i < numberSize; i++)
	{
		cin >> number[i];
	}*/

	
}